apiVersion: v1
kind: Service
metadata:
  name: k8s-app
  labels:
    app: k8s-app
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: k8s-app
  type: LoadBalancer
